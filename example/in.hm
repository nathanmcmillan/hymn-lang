#!/usr/bin/env hymn

let a = []
a[0] = 3
a[1] = 7
a[2] = 9

iterate v in a
  print v
end

iterate i, v in a
  print i + ": " + v
end

let t = {}
t.a = 3
t.b = 7
t.c = 9

iterate k in keys(t)
  print k
end

iterate i, k in keys(t)
  print i + ": " + k + " -> " + t[k]
end
