function fib(n, a, b)
  if n == 0 return a end
  return fib(n - 1, b, a + b)
end

let start = os.clock()

let N = 50000
let sum = 0

for i = 0, i < N
  let number = fib(1000, 0, 1)
  if number > 1
    sum += 1
  end
end

let seconds = os.clock() - start
echo "elapsed: " + seconds + "s | " + sum
