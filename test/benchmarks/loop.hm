let start = os_clock()

let N = 10000000
let list = []

for let i = 0; i < N; i = i + 1
  list[i] = i
end

let sum = 0

for let i = 0; i < N; i = i + 1
  sum = sum + list[i]
end

let seconds = os_clock() - start
print "elapsed: " + seconds + "s"
