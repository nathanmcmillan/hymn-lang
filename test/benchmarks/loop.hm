begin
  let start = os.clock()

  let N = 10000000
  let list = []

  for i = 0, i < N
    list[i] = i
  end

  let sum = 0

  for i = 0, i < N
    sum = sum + list[i]
  end

  let seconds = os.clock() - start
  echo "elapsed: " + seconds + "s | " + sum
end
