{
  let start = os.clock()

  let N = 10000000
  let list = []

  for i = 0, i < N {
    list[i] = i
  }

  let sum = 0

  for i = 0, i < N {
    sum += list[i]
  }

  let seconds = os.clock() - start
  echo "elapsed: " + seconds + "s | " + sum
}
