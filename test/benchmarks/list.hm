function reverse(a)
  let size = len(a)
  let max = math_floor(size / 2)
  for let i = 0; i < max; i = i + 1
    let n = size + 1 - i
    let t = a[i]
    a[i] = a[n]
    a[n] = t
  end
end

let start = os_clock()

let N = 20000000
let list = []

for let i = 1; i <= N; i = i + 1
  if N % i == 0
    push(list, i)
  end
end

reverse(list)

let seconds = os_clock() - start
print "elapsed: " + seconds + "s"
