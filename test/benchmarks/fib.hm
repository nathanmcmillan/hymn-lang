function fib(n)
  if n < 2 return n end
  return fib(n - 1) + fib(n - 2)
end

let start = os.clock()

let sum = 0

for i = 0, i < 10
  sum += fib(30)
end

let seconds = os.clock() - start
echo "elapsed: " + seconds + "s | " + sum
