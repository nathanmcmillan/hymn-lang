let start = os_clock()

let N = 50000000
let factors = []

for i = 1, i <= N
  if N % i == 0
    push(factors, i)
  end
end

let seconds = os_clock() - start
echo "elapsed: " + seconds + "s | " + factors[-1]
