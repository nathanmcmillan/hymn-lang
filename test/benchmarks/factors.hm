let start = os_clock()

let N = 50000000
let factors = []

-- for let i = 1; i <= N; i = i + 1
for i = 1, i <= N
  if N % i == 0
    push(factors, i)
  end
end

let seconds = os_clock() - start
print "elapsed: " + seconds + "s | " + factors[-1]
