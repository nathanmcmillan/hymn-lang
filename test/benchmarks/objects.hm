let start = os.clock()

let N = 500
let objects = []
let sum = 0
let words = ["foo", "bar", "baz"]

for i = 1, i < N
    let object = {}
    for word in words
      echo word
      object[word] = sum
      sum += i
    end
    push(objects, object)
end

let seconds = os.clock() - start
echo "elapsed: " + seconds + "s | " + objects[-1]
