#!/usr/bin/env hymn

let foo = 0

try { let mem = {} 10 + {} } except e { echo e }
try { let mem = {} 10 + [] } except e { echo e }

try { let mem = {} 10 * {} } except e { echo e }
try { let mem = {} 10 * [] } except e { echo e }

try { let mem = {} index({}, 0) } except e { echo e }
try { let mem = {} no } except e { echo e }
try { let mem = {} let arg = [] arg.foo } except e { echo e }
try { let mem = {} let arg = 0 len(arg) } except e { echo e }
try { let mem = {} let arg = 0 arg[0] } except e { echo e }
try { let mem = {} let arg = [] arg[0.0] } except e { echo e }
try { let mem = {} let arg = [] arg() } except e { echo e }
try { let mem = {} let arg = {} push(arg, 0) } except e { echo e }
try { let mem = {} let arg = {} pop(arg) } except e { echo e }
try { let mem = {} let arg = [] insert(arg, "", 0) } except e { echo e }
try { let mem = {} let arg = {} insert(arg, 0, 0) } except e { echo e }
try { let mem = {} let arg = [] insert(arg, -1, 0) } except e { echo e }
try { let mem = {} let arg = [] insert(arg, 10, 0) } except e { echo e }
try { let mem = {} let arg = [] delete(arg, "") } except e { echo e }
try { let mem = {} let arg = {} delete(arg, 0) } except e { echo e }
try { let mem = {} let arg = "" delete(arg, 0) } except e { echo e }
try { let mem = {} let arg = "" arg[:] } except e { echo e }
try { let mem = {} let arg = [] arg[10:20] } except e { echo e }
try { let mem = {} let arg = [] arg["":""] } except e { echo e }
try { let mem = {} let arg = [] keys(arg) } except e { echo e }
try { let mem = {} let arg = [] int(arg) } except e { echo e }
try { let mem = {} let arg = [] float(arg) } except e { echo e }
try { let mem = {} do [] } except e { echo e }
try { let mem = {} use [] } except e { echo e }

func bad() { let a = [0, 2, 4] }

try { let b = bad() b[0] = b[0] + 1 } except e { echo e }

echo "done!"
