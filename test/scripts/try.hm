#!/usr/bin/env hymn

use "assert"

let message = "empty"
try
  index("not-an-array", 0)
  message = "no"
except error
  message = error
end
assert_not("no", message)
assert_not("empty", message)

print message
print "done"
