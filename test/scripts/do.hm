do ""

do "print 1 + 2"
do "function foobar(n) print 'foo: ' + n end foobar(6)"

do read("test/scripts/std.hm")
do "print min(4, 6)"

# there's a memory leak when an import is not found, inside a do operation

do read("i-do-not-exist.hm")
