# paths: none, false
# imports: none, false
# globals: none
# imported

delete(GLOBALS, "PATHS")

echo "paths: " + GLOBALS.PATHS + ", " + exists(GLOBALS, "PATHS")

delete(GLOBALS, "IMPORTS")

echo "imports: " + GLOBALS.IMPORTS + ", " + exists(GLOBALS, "IMPORTS")

GLOBALS = none

echo "globals: " + GLOBALS

GLOBALS = none

use "import_me"
