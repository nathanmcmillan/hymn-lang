# foo
# bar
# end

function foo() {
  try {
    echo [2, 4, 8][3]
  } except _ {
    echo "foo"
  }
}

function bar() {
  try {
    throw "bad"
  } except _ {
  }
  echo "bar"
}

foo()
bar()
echo "end"
