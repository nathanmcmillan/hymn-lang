
# trim returns a string with spaces removed from the head and tail
func trim(s) {
  if len(s) == 0 {
      return s
  }
  while s[0] == ' ' {
      s = s[1:]
  }
  while s[-1] == ' ' {
      s = s[:-1]
  }
  return s
}
